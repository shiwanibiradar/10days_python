#print permutation of i. j in lexicographic order 
rows = int(input("Enter the no of rows"))
cols = int(input("Enter the no of cols"))

array=[[r,c] for r in range(rows+1) for c in range(cols+1)]
print(array)
